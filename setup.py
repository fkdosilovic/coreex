from setuptools import setup, find_packages

setup(
    name="coreex",
    version="0.1.0",
    description="Implementation of a content extraction algorithm named CoreEx.",
    packages=find_packages(include=["coreex"]),
)
